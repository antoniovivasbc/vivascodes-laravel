RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

RewriteEngine on

Options +FollowSymlinks

rewritecond %{http_host} ^vivasacessorios.com.br [NC]

rewriterule ^(.*)$ http://www.vivasacessorios.com.br/$1 [r=301,NC]